{
  "name": "api-testing",
  "version": "0.1.0",
  "description": "Bundle of API tests for Chabe Gateway",
  "main": "index.js",
  "scripts": {
    "test-dev": "NODE_ENV=dev mocha --timeout 25000 $(find test -name '*.js')",
    "test-staging": "NODE_ENV=staging mocha --timeout 25000 $(find test -name '*.js')"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.8.0",
    "mocha": "^7.1.0"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "config": "^3.3.0",
    "dotenv": "^8.2.0",
    "qs": "^6.9.1"
  }
}
